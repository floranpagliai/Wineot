{{ form_start(form) }}
        {% if errors  is defined %}
            {% for error in errors %}
                <div class="alert alert-danger"><i class="fa fa-warning"></i> {{ error.message|trans }}</div>
            {% endfor %}
        {% endif %}
<fieldset>
    <div class="form-group">
        <label class="sr-only" for="winery">Winery</label>
        {{ form_widget(form.winery, { 'attr': { 'placeholder': 'winery'|trans } } ) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.name, { 'attr': { 'placeholder': 'name'|trans } } ) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.description, { 'attr': { 'placeholder': 'description'|trans } } ) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.color, { 'attr': { 'placeholder': 'color'|trans } } ) }}
    </div>
    {% include 'WineotBackEndCRUDBundle:Vintage:form.html.twig' %}
    <br>
    <input class="btn btn-lg  btn-block" type="submit" id="_submit" name="submit_wine"
           value="{{ 'submit'|trans }}"/>
</fieldset>
<br>
{{ form_end(form) }}
{% block javascript %}

{% endblock %}